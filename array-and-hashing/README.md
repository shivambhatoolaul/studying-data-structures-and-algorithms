# 🔢 array-and-hashing

**Pattern:** Arrays store similar data in order and are used for sequential access problems. 

Hashing transforms a key into a hash to enable fast *O(1)* lookups. We use hash maps or sets for fast lookups, counting, or grouping element problems.
